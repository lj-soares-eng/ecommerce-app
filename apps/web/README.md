# Web (React)

E-commerce frontend. Notification button can consume WebSocket or SSE from API/dashboard for in-app notifications.

- **Run:** `npm run dev` (from repo root: `npm run dev:web`)
- **Default:** http://localhost:5173 (Vite)
