# Dashboard WS (Bun + WebSockets)

Real-time dashboard server. Bun for performance; WebSockets for live updates.

- **Run:** `bun run dev` (from repo root: `npm run dev:dashboard`)
- **Default:** ws://localhost:3001

Extend with auth and channels per dashboard/user as needed.
